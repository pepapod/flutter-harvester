import 'package:flutter/material.dart';

/// We should try to unify all the themes
/// available in the application
///
/// We should make a big effort to use this styling
/// instead of ad-hoc in each widget

class HarvesterTheme {
  static ThemeData theme = ThemeData(
    primarySwatch: Colors.green,
  );
}